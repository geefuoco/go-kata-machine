package katas

//Convert edgelist [['a', 'b'], ['c', 'd']] to graph 
//Graph: {'a': ['b'], b: ['a'], c: ['d'], d: ['c']}

func ConvertEdgelistToGraph(edgelist [][2]rune) Graph {
  var g Graph
  return g
}
